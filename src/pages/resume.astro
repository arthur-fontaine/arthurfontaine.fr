---
import resumeState from "../data/resume";

const resume = await resumeState.get();

return new Response(resume, {
  headers: {
    "Content-Type": "application/octet-stream",
    "Content-Disposition": "attachment; filename=Arthur Fontaine - CV.pdf",
  },
});
---